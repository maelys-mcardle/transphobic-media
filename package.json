{
  "name": "transphobic-media",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.3.1",
    "react-dom": "^16.3.1",
    "react-scripts": "1.1.4"
  },
  "devDependencies": {
    "node-fetch": "^2.1.2"
  },
  "scripts": {
    "start": "npm run build-db && react-scripts start",
    "build-db": "node db/generatejson.js",
    "build": "npm run build-db && react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
